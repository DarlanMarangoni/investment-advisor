# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**investment-advisor** — a Java application to support investment decisions (Portuguese: "Projeto criado para apoiar a decisao de investimento").

This project is in its initial state. No build tool (Maven/Gradle), source directories, or dependencies have been established yet.

## Tech Stack

- Language: Java
- IDE: IntelliJ IDEA (`.idea/` configuration present, module type `JAVA_MODULE`)

## Setup Notes

No build tool has been added yet. Once one is chosen, update this file with:
- How to build (e.g. `mvn package` / `./gradlew build`)
- How to run tests (e.g. `mvn test` / `./gradlew test`)
- How to run a single test class